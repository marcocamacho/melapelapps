package com.vonage.notify.web.client

import spock.lang.Specification

/**
 * Created by mcamacho on 9/23/15.
 */
class NotificationClientTest /*extends Specification */{
/*
    def "DoRequest"() {
        setup:
        NotificationClient client = Feign.builder()
                .logger(new Slf4jLogger())
                .encoder(new GsonEncoder())
                .decoder(new GsonDecoder())//.invocationHandlerFactory(new MyInvocationHandlerFactory)
                .target(NotificationClient, "http://blah.com" )

        when:
            client.doRequest("?param=444", new EventMessage());
        then:
            true

    }
    */
}
