package com.gamarlos.melapelapps.repository
/**
 */
class ConsumerRespositoryImpl /*implements ConsumerRepository*/{
/*
    MongoTemplate mongo;

    public ConsumerRespositoryImpl(MongoTemplate mongo){
        this.mongo = mongo;
    }

    @Override
    void saveConsumer(String id, String provider, String url) {
        mongo.save(new Consumer(id: id, provider: provider, url: url))
    }

    @Override
    Consumer getConsumer(String id) {
        mongo.findById(id, Consumer)
    }
    */
}
